# MonoSync 개발 정리

## 앱 개요

MonoSync는 Apple Music 구독자가 앱 안에서 음악을 듣고, 자신의 공간을 통해 친구들과 현재 재생 중인 음악 상태를 공유하는 iOS 앱이다. 현재 방향은 일반적인 스트리밍 앱보다 아날로그 카세트 플레이어에 가까운 경험으로 잡혀 있다.

현재 UI 표기명은 `momosync.`이며, 부제는 `share the tape.`이다.

## 핵심 컨셉

- Apple Music에서 곡/앨범/플레이리스트를 가져온다.
- 곡 단위가 아니라 앨범 또는 플레이리스트 단위를 카세트처럼 다룬다.
- 사용자는 앨범이나 플레이리스트를 `내 서랍`에 넣고, 이를 카세트의 `A면` 또는 `B면`에 삽입한다.
- 플레이어는 실제 카세트처럼 이전, 재생, 일시정지, 다음, 정지 버튼으로 조작한다.
- 친구의 공간에 들어가 현재 듣는 음악을 보고, 같은 곡을 따라 듣는 흐름을 목표로 한다.

## Apple Music으로 가능한 것

- Apple Music 권한 요청과 연결 상태 확인
- Apple Music 카탈로그 검색
- 앨범 검색 및 앨범 정보 표시
- 앨범의 곡 목록 조회
- Apple Music 곡 재생
- 앱 내부 플레이어 상태와 MusicKit 플레이어 상태 동기화
- 사용자의 Apple Music 보관함 플레이리스트 목록 조회
- 플레이리스트의 곡을 앱의 자체 구조로 가져와 재생

## Apple Music으로 제한되는 것

- 친구가 Apple Music 앱에서 듣는 음악을 앱이 자동으로 몰래 조회하는 것은 불가능하다.
- 친구가 MonoSync 앱을 통해 음악을 재생할 때만 현재 곡 정보를 앱 서버나 Firebase에 발행할 수 있다.
- Apple Music 플레이리스트를 앱에서 직접 편집하고 Apple Music 원본에 자동 반영하는 흐름은 제한적이다.
- MonoSync 내부 플레이리스트와 Apple Music 원본 플레이리스트는 분리해서 다루는 것이 안전하다.

## 현재 구현된 주요 기능

- SwiftUI 기반 iOS 앱 구조
- 앱 이름, 번들, 기본 언어, iOS 배포 설정 정리
- 한국어 기본 UI
- Paperlogy, Cafe24PROSlimAir 폰트 적용
- 모노크롬 기반 UI와 포인트 컬러 적용
- 카세트 테이프 이미지 기반 플레이어
- `tape_full.png` 카세트 이미지 사용
- `round.png` 릴 이미지 회전 애니메이션
- 재생 중일 때만 릴 회전
- 일시정지/정지 시 릴 정지
- 카세트 버튼 터치 영역 조정
- 버튼 클릭 효과음 적용
- 테이프 삽입 효과음 적용
- 무음 모드에서도 효과음 재생되도록 오디오 세션 설정
- 하단 탭 제거 후 우측 사이드 패널 방식으로 전환
- 상단 버튼 구성: 검색, 플레이리스트, 서랍, 메뉴
- Apple Music 미연결 상태에서는 빈 앨범 이미지 영역에서 연결 유도
- 친구들/라디오/설정 화면 기본 구조
- 플레이어/친구들/라디오/설정 헤더 높이 통일

## 카세트 동작 규칙

- 아무 버튼도 눌리지 않은 상태를 `제로 상태`로 둔다.
- 이전/재생/다음/정지 버튼은 동시에 눌리지 않는다.
- 일시정지는 재생 버튼과 동시에 눌릴 수 있다.
- 이전 버튼은 재생 중 2초 이후에는 현재 곡 처음으로 이동하고, 한 번 더 누르면 이전 곡으로 이동한다.
- 다음 버튼은 다음 곡으로 이동하며 마지막 곡에서는 제로 상태가 된다.
- 정지 버튼은 제로 상태에서 누르면 서랍을 열고, 다른 버튼이 눌린 상태에서는 모두 해제한다.

## 내 서랍

- Apple Music 검색 결과에서 앨범 단위로 서랍에 담을 수 있다.
- 서랍은 한 행에 두 장의 앨범이 보이도록 구성했다.
- A면/B면 드롭 영역을 상단에 고정했다.
- 앨범을 드래그해서 A면/B면에 넣을 수 있다.
- 앨범 카드 오른쪽 하단 삽입 아이콘을 누르면 A면 삽입, B면 삽입, 앨범 삭제 팝업이 열린다.
- 기존 long press 메뉴는 드래그와 충돌해서 제거했다.

## 플레이리스트 처리 방식

Apple Music 플레이리스트는 일반 앨범과 다르게 다룬다.

- 플레이리스트 목록 조회는 `MusicLibraryRequest<Playlist>`를 사용한다.
- 플레이리스트를 서랍에 넣을 때는 곡 전체를 즉시 펼치지 않는다.
- 서랍에는 Apple Music 플레이리스트 참조만 가볍게 저장한다.
- 실제 재생 시점에 원본 플레이리스트의 `Song` 목록을 가져와 MusicKit 큐에 직접 넣는다.
- 이 방식은 앱 시작 직후 플레이리스트를 불러올 때 앱이 멈추는 현상을 줄이기 위한 구조다.

## 오늘 확인한 플레이리스트 문제 원인

현상:

- 앱 시작 직후 플레이리스트를 불러오면 제목만 보이고 이미지가 비거나, A/B면에 넣어도 재생이 안 되거나, 앱이 멈추는 현상이 있었다.
- 반대로 친구들 화면에서 rin의 곡을 먼저 재생한 뒤 플레이리스트를 열면 정상 동작했다.

추정 원인:

- 친구 곡 재생은 `ApplicationMusicPlayer`에 실제 곡 큐를 넣고 `play()`를 호출하므로 MusicKit 원격 프로세스와 계정/토큰 컨텍스트가 완전히 깨어난다.
- 플레이리스트 경로는 앱 시작 직후 `Playlist.with(.tracks)`처럼 무거운 보관함 확장 조회를 먼저 실행해 콜드 상태에서 멈출 가능성이 높았다.
- Apple Music 플레이리스트는 앨범이 아니므로 자체 artwork가 없거나 늦게 해석될 수 있다. 그래서 이미지가 없다고 해서 반드시 실패는 아니다.

수정:

- 플레이리스트를 서랍에 넣는 순간 곡 전체를 가져오지 않도록 변경했다.
- 플레이리스트 재생은 `TrackSnapshot`을 다시 Apple Music에서 검색하지 않고, 원본 플레이리스트의 `Song` 목록을 직접 큐에 넣도록 변경했다.
- 플레이리스트 곡 조회에는 timeout을 걸어 무한 대기 가능성을 줄였다.
- 빌드 확인 결과 `BUILD SUCCEEDED`.

## Firebase 계획

Firebase는 실시간 공간 공유와 친구/댓글/공개 범위 저장에 사용한다.

저장할 핵심 재생 상태:

- `trackId`
- `position`
- `startedAt`
- `playbackState`
- `spaceOwnerId`
- `visibility`
- `updatedAt`

예상 컬렉션:

- `users`
- `spaces`
- `spaceEvents`
- `comments`
- `friendships`
- `radioRooms`

## 아직 남은 작업

- 실제 iPhone에서 플레이리스트 콜드 시작 재검증
- 플레이리스트 artwork가 없는 경우 첫 곡 artwork를 안전하게 lazy 로딩할지 결정
- A/B면에 들어간 플레이리스트의 곡 목록 표시 UX 정리
- Firebase 실시간 발행/구독 본격 구현
- 친구 스페이스에서 같이 듣기 기능 안정화
- 댓글 입력/표시 UI 구현
- 라디오 기능 기획 확정
- 앱 설정의 공개 범위/친구 설정 실제 저장
- 온보딩과 권한 안내 팝업 정리
- App Store 심사 기준에 맞춘 개인정보 처리방침/이용약관 정리

## 확인 명령

마지막으로 확인한 빌드 명령:

```sh
xcodebuild -project MonoSync.xcodeproj -scheme MonoSync -destination generic/platform=iOS build
```

결과:

```text
BUILD SUCCEEDED
```
